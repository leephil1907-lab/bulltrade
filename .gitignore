# Dependencies (the vendored web-push tree under vendor/ is intentionally committed)
node_modules/
!vendor/node_modules/

# Runtime data — NEVER commit (contains DB, JWT secret, KYC uploads, deposit proofs)
data/

# Environment / secrets
.env

# Internal session notes
SESSION.md

# Logs
*.log
npm-debug.log*
